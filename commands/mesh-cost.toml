description = "Show the live agentmesh cost dashboard: marginal AI-credit cost of each orchestrated MCP server"
prompt = "Run `node dashboard/cost-report.js` from the agentmesh plugin root (resolve the path from this plugin's own install location) and print its output verbatim as the response — do not reformat, summarize, or invent numbers on top of it. If the script errors (e.g. copilot binary not on PATH, or run from a client other than Copilot CLI), report the exact error and stop; do not fabricate a plausible-looking table instead. One shot: do not change mode, write flag files, or persist anything."
